---
title: "JuliaTest"
format: html
editor: visual
---

## Julia Engine Test

Test simple output

```{julia}
#| echo: false
rand(100, 100)
```

You can add options to executable code like this

```{julia}
#| echo: false
KnitrEngine.display("2")
```

The `echo: false` option disables the printing of code (only output is displayed).
